# Use a flat node_modules instead of pnpm's symlinked layout.
# Avoids Windows EBUSY symlink-lock errors (antivirus/file locks) and keeps
# resolution simple. pnpm-lock.yaml is still the source of truth.
node-linker=hoisted
